[project]
name = "nokogiri-org"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.12",
    "ghp-import>=2.1",
    "zensical>=0.0.15",
]
